function cd2 {


cd "$*"

PATH_DEPTH=$(pwd | awk -F/ '{print NF}')
if (( $PATH_DEPTH < 6 )); then
	CURDIR="$PWD"
else
	CURDIR="/$(pwd | cut -d/ -f2)/.../$(pwd | cut -d/ -f$(($PATH_DEPTH - 1))-)"
fi
export CURDIR
}
